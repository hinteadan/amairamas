(function (undefined) {
    'use strict';

    //function QueryCounters() {

    //}

    this.Counter = this.Counter || {};
    this.Counter.findAll = function () {
        
    };

    /*
    Counter.find().byId()
    Counter.findAll()
        .where('name')(ds.is.EqualTo)('danish')
        .where('name')(ds.is.EqualTo)('danish')
        .then(function(result){
        });
    */

}).call(this);